{
  "name": "hackathon",
  "version": "1.0.0",
  "scripts": {
      "dev:frontend": "cd frontend && npm run dev",
      "dev:backend": "cd backend && npm run dev",
      "dev": "npm-run-all --parallel dev:*"
  },
  "devDependencies": {
      "npm-run-all": "^4.1.5"
  }
}
